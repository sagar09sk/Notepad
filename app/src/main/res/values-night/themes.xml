<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Notepad" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">#5C6BC0</item>
        <item name="android:textColor">#FFFFFF</item>
        <!-- Background color -->
        <item name="android:windowBackground">@color/background_color_dark</item>
        <item name="android:colorBackground">@color/background_color_dark</item>
    </style>
</resources>