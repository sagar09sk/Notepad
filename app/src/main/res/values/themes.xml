<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Notepad" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">#5C6BC0</item>
        <item name="android:textColor">#000000</item>
        <!-- Background color -->
        <item name="android:windowBackground">@color/background_color_light</item>
        <item name="android:colorBackground">@color/background_color_light</item>

    </style>
</resources>